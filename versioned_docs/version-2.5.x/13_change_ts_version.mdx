---
sidebar_position: 13
---

# VS Code TS Version

1. Open VS Code and go to any `.ts`,`.d.ts` or `.tsx` file(important step!).
2. Press `Ctrl` + `Shift` + `P`.
3. Look for `Typescript: Select Typescript Version...`
4. Select `Use Workspace Version 5.x.x`.
