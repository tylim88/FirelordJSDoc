---
sidebar_position: 8
---

# Where 🍒

This section discuss how `where` and field path sentinel `documentId` works.

import { Image } from '@site/src/components/Image'


## Dealing With Literal Types 🦜

When working with literal types, due of Typescript limitation, you need `const` assertion in order to infers the type correctly.

<Image src='https://github.com/tylim88/FirelordJSDoc/blob/main/static/img/where1.png?raw=true' text={`const assertion is needed`}/>

## documentId() 🦜

When working with dield path sentinel `documentId()`, you also need `const` assertion for the same reason.

### Collection Reference

When querying a collection by documentId(), you must provide a plain document ID, without any forward slash.

<Image src='https://github.com/tylim88/FirelordJSDoc/blob/main/static/img/where2.png?raw=true' text={`const assertion is needed`}/>