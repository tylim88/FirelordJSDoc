---
sidebar_position: 5
---

# Tests 🧪

This section discuss how to use FirelordJS with [emulator](https://firebase.google.com/docs/emulator-suite), [@firebase/rules-unit-testing](https://www.npmjs.com/package/@firebase/rules-unit-testing) and [jest](https://jestjs.io/).

Remember to prepare script like this:

```json
{
	"test": "firebase emulators:exec --only database \"jest --forceExit\""
}
```

:::note
This is not a tutorial of `emulator`, `@firebase/rules-unit-testing` and `jest`, we assume you already know them.
:::

import { Image } from '@site/src/components/Image'

## With Emulator Only 🦜

Exmaple:

```ts

```

## With Both @firebase/rules-unit-testing And Emulator 🦜

Example:

```ts

```
